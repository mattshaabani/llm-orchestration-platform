# Redis settings
redis:
  host: "localhost"
  port: 6379
  db: 0
  cache_db: 1

# PostgreSQL settings
postgres:
  host: "localhost"
  port: 5432
  database: "llm_gateway"
  user: "gateway_user"

# Connection pool
pool:
  min_size: 2
  max_size: 10